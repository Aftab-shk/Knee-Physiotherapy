fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
Pillow==10.3.0
# Held at 1.x by the torch pin below, not by anything in this codebase. See the
# note there: the two versions are a pair and only move together.
numpy==1.26.4
opencv-python-headless==4.9.0.80

# torch + torchvision: installed via Dockerfile with CPU wheels
# for local dev run: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# These are a lockfile, not a compatibility claim. They are what the image
# installs and what the checked-in virtualenvs hold; a current machine resolves
# torch 2.12.1 with numpy 2.3.4 instead, and the whole suite passes on that too.
# Both are fine because each brings the numpy that matches it. torch before 2.5
# was built against NumPy 1, and pairing it with NumPy 2 fails nowhere near the
# mistake: imports succeed, inference succeeds, and the first .numpy() call
# raises "Numpy is not available" — which on this codebase means the Grad-CAM
# overlay throwing at request time (model/gradcam.py), not a training script.
#
# pip cannot express that coupling, so it is asserted in test_deployment.py
# against whatever is actually installed. Move these two together, or that test
# will say so.
torch==2.3.0
torchvision==0.18.0
pydantic==2.7.1
python-dotenv==1.0.1

# Accounts + persistence. SQLAlchemy talks to SQLite out of the box; point
# DATABASE_URL at Postgres for a real deployment. Argon2id is the password
# hasher (OWASP's first choice, and no silent 72-byte truncation like bcrypt).
# >=2.0.43: earlier releases crash resolving `Mapped[str | None]` on
# Python 3.13+, which is what a current dev machine will have.
SQLAlchemy==2.0.52
# The Postgres driver for the DATABASE_URL a deployment actually uses
# (postgresql+psycopg://…). SQLAlchemy does not ship one, so without this the
# app starts fine on SQLite in development and dies at import in production.
psycopg[binary]==3.2.3
PyJWT==2.10.1
argon2-cffi==23.1.0
email-validator==2.2.0

# Schema migrations. init_db() runs `upgrade head` at startup, so development
# and deployment take the same path and cannot drift. See backend/migrations/.
alembic==1.14.0

# The appointment summary PDF. Pure Python, no system libraries — which is why
# it is here and not WeasyPrint, whose Cairo/Pango stack would roughly double
# the image for one endpoint.
reportlab==5.0.1

# dev only — `python -m pytest backend/tests -q`
pytest==8.2.0
# Reads the generated PDF back as text, so the summary tests assert what a
# reader sees rather than what the drawing code was asked to draw.
pypdf==6.17.0
