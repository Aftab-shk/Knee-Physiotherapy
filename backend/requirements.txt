fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80
# torch + torchvision: installed via Dockerfile with CPU wheels
# for local dev run: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.3.0
torchvision==0.18.0
pydantic==2.7.1
python-dotenv==1.0.1

# Accounts + persistence. SQLAlchemy talks to SQLite out of the box; point
# DATABASE_URL at Postgres for a real deployment. Argon2id is the password
# hasher (OWASP's first choice, and no silent 72-byte truncation like bcrypt).
# >=2.0.43: earlier releases crash resolving `Mapped[str | None]` on
# Python 3.13+, which is what a current dev machine will have.
SQLAlchemy==2.0.52
PyJWT==2.10.1
argon2-cffi==23.1.0
email-validator==2.2.0

# Schema migrations. init_db() runs `upgrade head` at startup, so development
# and deployment take the same path and cannot drift. See backend/migrations/.
alembic==1.14.0

# dev only — `python -m pytest backend/tests -q`
pytest==8.2.0
